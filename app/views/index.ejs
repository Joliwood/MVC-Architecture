<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="stylesheets/style.css"></link>
    <title>Accueil</title>
    <script>
      // Function to refresh the page after a delay (in milliseconds)
      setTimeout(function() {
        location.reload();
      }, 1000); // Refresh after 1 second (adjust the delay as needed)
    </script>
  </head>
  <%- include("partials/header") %>
  <body class="bg-white min-h-screen	">
    <h1>It is working</h1>
    <section class="flex bg-gray-200 justify-center	p-5">
      <%- include("partials/card") %>
    </section>
    <%# allQuestions.forEach(element => { %>
    <h3><%# element.description %> <%- oneQuestionWithId3.description %></h3>
    <%# }); %>
    <a href="/signup">S'enregistrer</a>
  </body>
</html>
